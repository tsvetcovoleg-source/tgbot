-- Добавляет колонку `status` в существующую таблицу `users`.
-- Выполните этот SQL в phpMyAdmin (или другой клиент), чтобы сохранить текущие данные.

ALTER TABLE `users`
    ADD COLUMN `status` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '1 — новый пользователь, 2 — получил ответ на неизвестное сообщение';

-- Если колонка уже существует, phpMyAdmin сообщит об ошибке. В этом случае ничего делать не нужно.
-- При необходимости обновить существующих пользователей на статус 1, выполните:
-- UPDATE `users` SET `status` = 1 WHERE `status` IS NULL;
